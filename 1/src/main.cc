#include "exercises.h"

int main() {
	/*
	auto sum = exercises::get_sum(); 
	exercises::print_sum(sum); 

	exercises::count_repeated_inputs(); 
	int a = 1;
	int b = 2;
	exercises::swap(&a, &b);
	std::cout << "a " << a << " " << " b " <<  b << std::endl;
	*/
	return 0;
}
