#pragma once

namespace exercises {
	// Write code to change the value of a pointer. 
	int* change_pointer_value(int* original_ptr, int* new_ptr);
	// Write code to change the value to which the pointer points 
	int change_pointed_value(int* original_ptr, int new_value);
	// Mmutliply a number using derefernce
	int square_using_pointer(int value);
}
